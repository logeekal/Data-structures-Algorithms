export default class GraphEdge {
    constructor(fromNode, toNode, weight) {
        this.fromNode = fromNode;
        this.toNode = toNode;
        this.weight = weight
    }

    
    this.edgeExists = fromNode, toNode,  => {
        if(this.fromNode.isEqual(fromNode))
    }

    this.removeEdge = node1, node2 => {
        if edgeExists(node1, node2){
            this.nodes.length = 0;
            return true;
        }

    throw new Error(`Node edge exists between ${node1} and ${node2}`);
}

}